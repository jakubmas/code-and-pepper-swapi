# Database
DATABASE_URL=postgresql://username:password@hostname:5432/starwars_battle

# Server
PORT=4000

# Environment (development | production)
# In production, SSL certificates are strictly verified for security
# In development, self-signed certificates are allowed
NODE_ENV=development