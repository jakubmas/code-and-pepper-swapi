overwrite: true
schema: "./src/schema/graphql/type-defs.ts"
generates:
  src/types/generated.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      skipTypename: true
      enumsAsTypes: true
      contextType: "./graphql-context#GraphQLContext"
      scalars:
        Date: string